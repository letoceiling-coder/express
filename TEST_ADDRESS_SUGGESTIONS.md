# Тестирование автокомплита адресов (Яндекс Suggest API)

## ✅ Предварительные проверки

### 1. Проверка API ключа

Убедитесь, что API ключ заполнен в настройках:
- Откройте админ-панель: `/admin/settings/delivery`
- Проверьте, что поле "API ключ Яндекс.Геокодер" заполнено
- Ключ должен быть сохранен в БД (таблица `delivery_settings`, поле `yandex_geocoder_api_key`)

---

## 🧪 Варианты тестирования

### Вариант 1: Тест через браузер (Frontend)

1. **Откройте страницу оформления заказа:**
   - В мини-приложении перейдите на страницу оформления заказа
   - Выберите "Доставка курьером"

2. **Введите адрес:**
   - Начните вводить адрес в поле "Адрес доставки"
   - Введите минимум 2 символа (например: "Ле")
   - Подождите 300ms (debounce)

3. **Проверьте результат:**
   - Должны появиться подсказки под полем ввода
   - Подсказки должны содержать адреса в Екатеринбурге
   - При клике на подсказку адрес должен заполниться в поле

4. **Откройте консоль браузера (F12):**
   - Перейдите на вкладку "Console"
   - Проверьте наличие ошибок
   - Должны быть логи запросов к API

---

### Вариант 2: Тест через API напрямую (curl/Postman)

#### Тест 1: Базовый запрос

```bash
curl -X POST http://express.loc/api/v1/delivery/address-suggestions \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Ленина",
    "city": "Екатеринбург"
  }'
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "suggestions": [
    {
      "value": "г. Екатеринбург, ул. Ленина, 10",
      "display": "ул. Ленина, 10",
      "subtitle": "г. Екатеринбург"
    },
    ...
  ]
}
```

#### Тест 2: Без указания города

```bash
curl -X POST http://express.loc/api/v1/delivery/address-suggestions \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Мира"
  }'
```

#### Тест 3: Ошибка валидации (слишком короткий запрос)

```bash
curl -X POST http://express.loc/api/v1/delivery/address-suggestions \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Л"
  }'
```

**Ожидаемый ответ (422):**
```json
{
  "message": "Запрос должен содержать минимум 2 символа",
  "errors": {
    "query": ["Запрос должен содержать минимум 2 символа"]
  }
}
```

#### Тест 4: Отсутствие API ключа

Если API ключ не заполнен:
```json
{
  "success": false,
  "error": "API ключ не настроен",
  "suggestions": []
}
```

---

### Вариант 3: Тест через Laravel Tinker

```bash
php artisan tinker
```

```php
// Проверить наличие ключа
$settings = \App\Models\DeliverySetting::getSettings();
echo $settings->yandex_geocoder_api_key ? "Ключ есть" : "Ключа нет";

// Тест запроса к контроллеру
$request = \Illuminate\Http\Request::create('/api/v1/delivery/address-suggestions', 'POST', [
    'query' => 'Ленина',
    'city' => 'Екатеринбург'
]);
$controller = new \App\Http\Controllers\Api\v1\DeliverySettingsController();
$response = $controller->getAddressSuggestions($request);
$response->getContent();
```

---

## 🔍 Проверка логов

### Логи Laravel

Проверьте логи на наличие ошибок:

```bash
tail -f storage/logs/laravel.log
```

Или откройте файл: `storage/logs/laravel.log`

**Что искать:**
- Ошибки подключения к Яндекс API
- Ошибки валидации
- Ошибки обработки ответа от API

### Логи браузера

В консоли браузера (F12 → Console) проверьте:
- Запросы к `/api/v1/delivery/address-suggestions`
- Ответы от API
- Ошибки JavaScript

---

## 📋 Чек-лист тестирования

- [ ] API ключ заполнен в настройках
- [ ] Роут доступен (`/api/v1/delivery/address-suggestions`)
- [ ] Запрос с валидными данными возвращает подсказки
- [ ] Запрос с 1 символом возвращает ошибку валидации
- [ ] Запрос без API ключа возвращает ошибку
- [ ] Frontend показывает подсказки при вводе адреса
- [ ] Подсказки отфильтрованы по городу (Екатеринбург)
- [ ] Клик по подсказке заполняет поле адреса
- [ ] Нет ошибок в логах Laravel
- [ ] Нет ошибок в консоли браузера

---

## 🐛 Типичные проблемы и решения

### Проблема 1: "API ключ не настроен"

**Причина:** Ключ не заполнен в БД

**Решение:**
1. Откройте `/admin/settings/delivery`
2. Заполните поле "API ключ Яндекс.Геокодер"
3. Сохраните настройки

### Проблема 2: "Ошибка при получении подсказок" (500)

**Причины:**
- Неверный API ключ
- Проблемы с подключением к Яндекс API
- Превышен лимит запросов

**Решение:**
1. Проверьте логи Laravel (`storage/logs/laravel.log`)
2. Проверьте валидность API ключа
3. Проверьте интернет-соединение
4. Проверьте лимиты в личном кабинете Яндекс

### Проблема 3: Подсказки не появляются на frontend

**Причины:**
- Ошибка JavaScript
- Неправильный URL API
- Проблемы с CORS

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки
3. Проверьте вкладку Network - есть ли запрос к API
4. Проверьте ответ сервера

### Проблема 4: Пустой список подсказок

**Причины:**
- Адрес не найден в Яндекс API
- Фильтр по городу слишком строгий
- Проблемы с обработкой ответа

**Решение:**
1. Попробуйте другой адрес
2. Проверьте ответ от Яндекс API в логах
3. Проверьте фильтрацию по городу в коде

---

## ✅ Успешный тест

При успешном тестировании вы должны увидеть:

1. **В браузере:**
   - При вводе адреса появляются подсказки
   - Подсказки содержат адреса в Екатеринбурге
   - Клик по подсказке заполняет поле

2. **В API ответе:**
   ```json
   {
     "success": true,
     "suggestions": [
       {
         "value": "г. Екатеринбург, ул. Ленина, 10",
         "display": "ул. Ленина, 10",
         "subtitle": "г. Екатеринбург"
       }
     ]
   }
   ```

3. **В логах:**
   - Нет ошибок
   - Запросы к Яндекс API успешны (статус 200)

---

## 📝 Примеры тестовых запросов

### Екатеринбург

```json
{"query": "Ленина", "city": "Екатеринбург"}
{"query": "Мира", "city": "Екатеринбург"}
{"query": "пр. Ленина", "city": "Екатеринбург"}
{"query": "ул. Сакко и Ванцетти", "city": "Екатеринбург"}
```

### Другие города (для проверки)

```json
{"query": "Тверская", "city": "Москва"}
{"query": "Невский", "city": "Санкт-Петербург"}
```

---

## 🔧 Отладка

### Включить подробное логирование

В `app/Http/Controllers/Api/v1/DeliverySettingsController.php` можно добавить:

```php
Log::info('Address suggestions request', [
    'query' => $query,
    'city' => $city,
    'searchQuery' => $searchQuery,
]);

Log::info('Yandex API response', [
    'status' => $response->status(),
    'data' => $data,
]);
```

### Проверка ответа от Яндекс API

Добавьте временный лог в контроллере:

```php
Log::debug('Yandex Suggest API raw response', [
    'body' => $response->body(),
]);
```

