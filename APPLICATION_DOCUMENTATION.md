# Документация приложения Laravel + React + Vue

**Важно:** Эта документация должна обновляться при любых изменениях в приложении. Все изменения в логике, структуре, API или визуальной части должны быть отражены в этом документе.

---

## 📋 Содержание

1. [Обзор архитектуры](#обзор-архитектуры)
2. [Backend (Laravel)](#backend-laravel)
3. [Frontend - React MiniApp](#frontend-react-miniapp)
4. [Frontend - Vue Admin Panel](#frontend-vue-admin-panel)
5. [Интеграции](#интеграции)
6. [Правила работы с Cursor](#правила-работы-с-cursor)
7. [Шаблоны промптов](#шаблоны-промптов)

---

## 🏗 Обзор архитектуры

### Структура проекта

```
express/
├── app/                        # Laravel Backend
│   ├── Console/Commands/      # Artisan команды
│   ├── Http/
│   │   ├── Controllers/       # API контроллеры
│   │   ├── Middleware/        # Middleware
│   │   ├── Requests/          # Валидация запросов
│   │   └── Resources/         # API Resources
│   ├── Models/                # Eloquent модели
│   ├── Services/              # Бизнес-логика
│   └── Observers/             # Модель-наблюдатели
├── frontend/                  # React MiniApp
│   └── src/
│       ├── components/        # React компоненты
│       ├── pages/             # Страницы приложения
│       ├── store/             # Zustand stores
│       ├── api/               # API клиент
│       └── lib/               # Утилиты
├── resources/js/              # Vue Admin Panel
│   ├── pages/admin/          # Страницы админки
│   └── components/           # Vue компоненты
├── routes/
│   ├── api.php               # API маршруты
│   └── web.php               # Web маршруты
└── public/                   # Публичная директория
    ├── build/                # Собранная Vue админка
    └── frontend/             # Собранная React MiniApp
```

### Технологический стек

**Backend:**
- Laravel 11
- PHP 8.2+
- MySQL/PostgreSQL
- Laravel Sanctum (аутентификация)

**Frontend MiniApp:**
- React 18 + TypeScript
- React Router v6
- Zustand (state management)
- TanStack Query (data fetching)
- TailwindCSS
- shadcn/ui (UI компоненты)

**Frontend Admin:**
- Vue 3 + TypeScript
- Vue Router
- TailwindCSS
- Vite

**Интеграции:**
- Telegram Bot API
- Telegram Mini App API
- YooKassa (платежи)

---

## 🔧 Backend (Laravel)

### Структура директорий

#### `app/Models/` - Eloquent модели

**Основные модели:**

1. **User** - Пользователи системы
   - Связи: `roles` (many-to-many), `orders`, `notifications`
   - Traits: `HasUserScope` (автоматическая фильтрация по пользователю)

2. **Category** - Категории товаров
   - Поля: `name`, `slug`, `is_active`, `sort_order`, `image_id`
   - Связи: `image` (belongsTo Media), `products` (hasMany)

3. **Product** - Товары
   - Поля: `name`, `slug`, `description`, `price`, `stock_quantity`, `is_available`, `category_id`, `image_id`, `video_id`
   - Связи: `category`, `image`, `video`, `orderItems`, `productHistory`
   - Observer: `ProductObserver` (автоматическое создание истории изменений)

4. **Order** - Заказы
   - Поля: `order_id` (уникальный), `user_id`, `bot_id`, `telegram_id`, `phone`, `name`, `delivery_address`, `delivery_time`, `comment`, `total_amount`, `status`
   - Статусы: `pending`, `accepted`, `preparing`, `ready_for_delivery`, `in_transit`, `delivered`, `cancelled`
   - Связи: `user`, `items` (hasMany OrderItem), `delivery`, `payments`

5. **OrderItem** - Элементы заказа
   - Поля: `order_id`, `product_id`, `quantity`, `price`
   - Связи: `order`, `product`

6. **Media** - Медиа-файлы
   - Поля: `name`, `path`, `type`, `size`, `folder_id`, `user_id`
   - Поддержка WebP с вариантами размеров (thumbnail, medium, large)
   - Связи: `folder`, `user`

7. **Folder** - Папки медиа-библиотеки
   - Поля: `name`, `parent_id`, `sort_order`
   - Древовидная структура (self-referencing)

8. **Bot** - Telegram боты
   - Поля: `name`, `token`, `username`, `webhook_url`, `webhook_registered`, `welcome_message`, `settings` (JSON), `is_active`
   - Связи: `telegramUsers` (hasMany TelegramUser)

9. **TelegramUser** - Пользователи Telegram бота
   - Поля: `bot_id`, `telegram_id`, `first_name`, `last_name`, `username`, `language_code`, `is_premium`, `is_blocked`, `last_interaction_at`, `orders_count`, `total_spent`, `metadata` (JSON)
   - Связи: `bot` (belongsTo Bot), `orders` (hasMany через telegram_id)
   - Scopes: `active()`, `byBot($botId)`, `recent($days)`
   - Методы: `block()`, `unblock()`, `updateInteraction()`, `getFullNameAttribute()`

**Дополнительные модели:**
- `Payment` - Платежи
- `Delivery` - Доставки
- `Review` - Отзывы
- `Complaint` - Претензии
- `ProductReturn` - Возвраты
- `ProductHistory` - История изменений товаров
- `Notification` - Уведомления
- `Role` - Роли пользователей
- `SupportTicket`, `SupportMessage` - Система поддержки

#### `app/Http/Controllers/Api/v1/` - API контроллеры

**Основные контроллеры:**

1. **CategoryController**
   - `index()` - Список категорий (публичный GET)
   - `show($id)` - Детали категории (публичный GET)
   - `store()` - Создание (требует auth)
   - `update()` - Обновление (требует auth)
   - `destroy()` - Удаление (требует auth)

2. **ProductController**
   - `index()` - Список товаров с фильтрацией (публичный GET)
   - `show($id)` - Детали товара (публичный GET)
   - `store()` - Создание (требует auth)
   - `update()` - Обновление (требует auth)
   - `destroy()` - Удаление (требует auth)

3. **OrderController**
   - `index()` - Список заказов
   - `store()` - Создание заказа (для miniApp)
   - `show($id)` - Детали заказа
   - `updateStatus()` - Изменение статуса заказа
   - Интеграция с TelegramMiniAppService для уведомлений

4. **MediaController**
   - Автоматическая оптимизация изображений (WebP, варианты размеров)
   - `store()` - Загрузка с автоматической обработкой
   - Поддержка корзины (soft delete)

5. **TelegramController**
   - `validateInitData()` - Валидация Telegram initData
   - `getUserInfo()` - Получение информации о пользователе
   - `notifyOrder()` - Уведомление о заказе

**Структура ответов API:**

Все API endpoints возвращают JSON в формате:
```json
{
  "data": {...}
}
```

Или для списков:
```json
{
  "data": [...]
}
```

#### `app/Services/` - Бизнес-логика

1. **TelegramService**
   - `getBotInfo($token)` - Получение информации о боте
   - `setWebhook($token, $url, $options)` - Установка webhook
   - `sendMessage($token, $chatId, $text, $options)` - Отправка сообщения

2. **TelegramMiniAppService**
   - `validateInitData($initData, $botToken)` - Валидация initData
   - `getUserFromInitData($initData, $botToken)` - Извлечение пользователя
   - `notifyNewOrder($order, $botId)` - Уведомление о новом заказе
   - `notifyOrderStatusChange($order, $oldStatus, $newStatus)` - Уведомление об изменении статуса

3. **ImageService**
   - Автоматическая конвертация в WebP
   - Генерация вариантов размеров (thumbnail: 300x300, medium: 800x800, large: 1200x1200)
   - Оптимизация изображений

4. **SubscriptionService**
   - Проверка подписки через внешний API
   - Кеширование результатов

5. **Payment/YooKassaService**
   - Создание платежей
   - Обработка webhook'ов
   - Проверка статусов платежей

6. **TelegramUserService** (`app/Services/Telegram/TelegramUserService.php`)
   - `syncUser($botId, $telegramUserData)` - Синхронизация пользователя из Telegram
   - `updateUserFromTelegram($user)` - Обновление данных пользователя из Telegram API
   - `updateStatistics($user)` - Обновление статистики пользователя (orders_count, total_spent)

7. **BroadcastService** (`app/Services/Telegram/BroadcastService.php`)
   - `sendBroadcast($botId, $telegramUserIds, $type, $content, $options)` - Рассылка сообщений/медиа
   - `sendToAll($botId, $type, $content, $options)` - Рассылка всем пользователям бота
   - `sendToSelected($botId, $telegramUserIds, $type, $content, $options)` - Рассылка выбранным пользователям
   - Поддержка типов: `message`, `photo`, `document`, `media_group`
   - Автоматическая обработка ошибок и блокировка пользователей

8. **TelegramService** (расширен)
   - `sendPhoto($token, $chatId, $photo, $options)` - Отправка фото
   - `sendDocument($token, $chatId, $document, $options)` - Отправка документа
   - `sendMediaGroup($token, $chatId, $media, $options)` - Отправка группы медиа
   - `getChat($token, $chatId)` - Получение информации о чате

#### `routes/api.php` - API маршруты

**Структура маршрутов:**

```php
// Публичные роуты (без аутентификации)
Route::prefix('auth')->group(function () {
    // Регистрация, вход, восстановление пароля
});

// Публичные роуты для miniApp (GET запросы)
Route::prefix('v1')->group(function () {
    Route::get('categories', ...);      // Публичный
    Route::get('categories/{id}', ...); // Публичный
    Route::get('products', ...);        // Публичный
    Route::get('products/{id}', ...);   // Публичный
});

// Защищенные роуты (требуют auth:sanctum)
Route::middleware('auth:sanctum')->group(function () {
    // Все CRUD операции для категорий и товаров (POST, PUT, DELETE)
    // Заказы, медиа, пользователи и т.д.
});

// Специальные роуты
Route::middleware('deploy.token')->group(function () {
    // Деплой, интеграции
});
```

**Важные особенности:**
- GET запросы к категориям и продуктам - публичные (для miniApp)
- POST/PUT/DELETE запросы - требуют аутентификацию
- Все API endpoints имеют префикс `/api/v1/`

#### `app/Http/Middleware/` - Middleware

1. **ForceHttps** - Принудительное использование HTTPS
2. **CheckSubscription** - Проверка подписки
3. **EnsureUserIsAdmin** - Проверка прав администратора
4. **ValidateTelegramInitData** - Валидация Telegram initData
5. **VerifyDeployToken** - Проверка токена деплоя

---

## 📱 Frontend - React MiniApp

### Структура директорий

```
frontend/src/
├── components/
│   ├── miniapp/              # Компоненты для MiniApp
│   │   ├── MiniAppHeader.tsx    # Шапка приложения
│   │   ├── BottomNavigation.tsx # Нижняя навигация
│   │   ├── ProductCard.tsx      # Карточка товара
│   │   ├── CartItem.tsx         # Элемент корзины
│   │   ├── CategoryTabs.tsx     # Вкладки категорий
│   │   ├── OrderCard.tsx        # Карточка заказа
│   │   └── MiniAppLayout.tsx    # Layout компонент
│   ├── admin/                # Компоненты для админки (в React)
│   ├── ui/                   # shadcn/ui компоненты
│   └── OptimizedImage.tsx    # Компонент для оптимизированных изображений
├── pages/
│   ├── miniapp/              # Страницы MiniApp
│   │   ├── CatalogPage.tsx      # Каталог товаров (/)
│   │   ├── ProductDetailPage.tsx # Детали товара (/product/:id)
│   │   ├── CartPage.tsx         # Корзина (/cart)
│   │   ├── CheckoutPage.tsx     # Оформление заказа (/checkout)
│   │   ├── OrderSuccessPage.tsx # Успешный заказ (/order-success/:id)
│   │   ├── OrdersPage.tsx       # Список заказов (/orders)
│   │   └── OrderDetailPage.tsx  # Детали заказа (/orders/:id)
│   └── admin/                # Страницы админки (в React)
├── store/                    # Zustand stores
│   ├── cartStore.ts          # Состояние корзины
│   └── ordersStore.ts        # Состояние заказов
├── api/                      # API клиент
│   └── index.ts              # Все API функции
├── lib/                      # Утилиты
│   ├── telegram.ts           # Интеграция с Telegram WebApp API
│   └── utils.ts              # Общие утилиты
└── types/                    # TypeScript типы
    └── index.ts
```

### Маршрутизация (React Router)

**MiniApp маршруты:**
- `/` - CatalogPage (каталог товаров)
- `/product/:productId` - ProductDetailPage (детали товара)
- `/cart` - CartPage (корзина)
- `/checkout` - CheckoutPage (оформление заказа)
- `/order-success/:orderId` - OrderSuccessPage (успешный заказ)
- `/orders` - OrdersPage (список заказов)
- `/orders/:orderId` - OrderDetailPage (детали заказа)

**Admin маршруты (в React):**
- `/admin` - AdminLayout с вложенными маршрутами
  - `/admin` - AdminDashboard
  - `/admin/orders` - AdminOrders
  - `/admin/products` - AdminProducts
  - `/admin/categories` - AdminCategories

### Состояние приложения (Zustand)

#### `cartStore.ts` - Состояние корзины

```typescript
interface CartStore {
  items: CartItem[];
  addItem: (product: Product, quantity: number) => void;
  removeItem: (productId: string) => void;
  updateQuantity: (productId: string, quantity: number) => void;
  clearCart: () => void;
  getTotalAmount: () => number;
  getTotalItems: () => number;
}
```

**Использование:**
- Сохраняется в localStorage
- Автоматически синхронизируется
- Используется на страницах: CatalogPage, ProductDetailPage, CartPage, CheckoutPage

#### `ordersStore.ts` - Состояние заказов

```typescript
interface OrdersStore {
  orders: Order[];
  loading: boolean;
  error: string | null;
  fetchOrders: () => Promise<void>;
  createOrder: (payload: CreateOrderPayload) => Promise<Order>;
  getOrderById: (id: string) => Order | undefined;
}
```

### API клиент (`api/index.ts`)

**Функции:**

1. **categoriesAPI**
   - `getAll()` - Получение всех категорий
   - Использует `per_page=0` для получения всех записей

2. **productsAPI**
   - `getAll(categoryId?)` - Получение всех товаров (опционально по категории)
   - `getById(id)` - Получение товара по ID
   - Фильтрует только доступные товары (`is_available=true`)

3. **ordersAPI**
   - `getAll()` - Получение всех заказов
   - `getById(id)` - Получение заказа по ID
   - `create(payload)` - Создание заказа

**Особенности:**
- Автоматическое добавление токена авторизации из localStorage
- Обработка ошибок с выводом сообщений
- Поддержка разных форматов ответа API (массив, объект с data, пагинация)

### Интеграция с Telegram (`lib/telegram.ts`)

**Функции:**

1. **initTelegramWebApp()** - Инициализация Telegram WebApp API
2. **getTelegramUser()** - Получение данных пользователя Telegram
3. **hapticFeedback(type)** - Тактильная обратная связь
4. **showTelegramConfirm(message, callback)** - Подтверждение через Telegram
5. **closeTelegramApp()** - Закрытие приложения

**Использование:**
- Инициализация в `main.tsx`
- Использование на всех страницах miniApp
- Автоматическое определение темы (light/dark)

### Компоненты MiniApp

#### MiniAppHeader
- Отображается на всех страницах miniApp
- Параметры: `title`, `showBack`, `showCart`, `showThemeToggle`
- Показывает счетчик товаров в корзине

#### BottomNavigation
- Фиксированная нижняя навигация
- Три пункта: Каталог, Корзина, Заказы
- Подсветка активного маршрута
- Бейдж с количеством товаров в корзине

#### ProductCard
- Отображение товара в каталоге
- Показывает изображение, название, описание, цену
- Кнопка "Добавить в корзину"
- Использует OptimizedImage для изображений

#### OptimizedImage
- Компонент для оптимизированных изображений
- Автоматический выбор размера (thumbnail, medium, large)
- Fallback на оригинальное изображение
- Поддержка WebP с fallback на JPG

### Страницы MiniApp

#### CatalogPage (`/`)
- Отображает категории (вкладки вверху)
- Сетка товаров (2 колонки)
- Фильтрация по категориям
- Использует `useProducts` hook для загрузки данных
- При клике на товар - переход на ProductDetailPage

#### ProductDetailPage (`/product/:productId`)
- Детальная информация о товаре
- Галерея изображений
- Кнопки добавления в корзину (+/-)
- Показывает количество в корзине
- Кнопка "Добавить в корзину"

#### CartPage (`/cart`)
- Список товаров в корзине
- Изменение количества
- Удаление товаров
- Итоговая сумма
- Кнопка "Оформить заказ" (переход на /checkout)
- Кнопка "Очистить корзину" с подтверждением

#### CheckoutPage (`/checkout`)
- Трехшаговый процесс оформления заказа
- Шаг 1: Телефон и имя
- Шаг 2: Адрес и время доставки
- Шаг 3: Комментарий и итоговая информация
- Валидация на каждом шаге
- Автозаполнение имени из Telegram
- После успешного заказа - переход на OrderSuccessPage

#### OrderSuccessPage (`/order-success/:orderId`)
- Отображение успешного создания заказа
- Номер заказа
- Информация о заказе
- Кнопка "Вернуться в каталог"

#### OrdersPage (`/orders`)
- Список всех заказов пользователя
- Использует `useOrders` hook
- Карточки заказов (OrderCard)
- При клике - переход на OrderDetailPage

#### OrderDetailPage (`/orders/:orderId`)
- Детальная информация о заказе
- Список товаров
- Статус заказа
- Информация о доставке
- Итоговая сумма

---

## 🎨 Frontend - Vue Admin Panel

### Структура директорий

```
resources/js/
├── pages/admin/              # Страницы админки
│   ├── Dashboard.vue         # Главная страница
│   ├── Categories.vue        # Список категорий
│   ├── CategoryCreate.vue    # Создание категории
│   ├── CategoryEdit.vue      # Редактирование категории
│   ├── Products.vue          # Список товаров
│   ├── ProductCreate.vue     # Создание товара
│   ├── ProductEdit.vue       # Редактирование товара
│   ├── ProductHistory.vue    # История изменений товара
│   ├── Orders.vue            # Список заказов
│   ├── OrderDetail.vue       # Детали заказа
│   ├── Media.vue             # Медиа-библиотека
│   ├── Users.vue             # Пользователи
│   ├── Roles.vue             # Роли
│   ├── Bots.vue              # Управление ботами
│   └── ...                   # Другие страницы
├── components/               # Vue компоненты
└── admin.js                  # Точка входа
```

### Маршрутизация

Все маршруты админки имеют префикс `/admin`

**Основные маршруты:**
- `/admin` - Dashboard
- `/admin/categories` - Категории
- `/admin/products` - Товары
- `/admin/orders` - Заказы
- `/admin/media` - Медиа-библиотека
- `/admin/users` - Пользователи
- `/admin/roles` - Роли
- `/admin/bots` - Боты

### API клиент (`utils/api.js`)

- Использует axios
- Автоматическое добавление токена из localStorage
- Обработка ошибок
- База: `/api/v1`

### Система меню

Меню генерируется динамически через `AdminMenuService`:
- Фильтрация по ролям пользователя
- Иерархическая структура
- Группировка (Каталог, Заказы, Обратная связь)

---

## 🔗 Интеграции

### Telegram Bot

**Конфигурация:** `config/telegram.php`

**Основные настройки:**
- `bot_token` - Токен бота
- `mini_app_url` - URL miniApp
- `webhook_url` - URL для webhook

**Функциональность:**

1. **Регистрация ботов** (`BotController`)
   - Создание бота через API
   - Автоматическая установка webhook
   - Сохранение настроек

2. **Обработка команд** (`BotController::handleWebhook`)
   - `/start` - Приветственное сообщение с кнопкой для запуска miniApp
   - Кнопка использует `web_app` тип для открытия miniApp

3. **Валидация initData** (`TelegramMiniAppService`)
   - Валидация данных от Telegram MiniApp
   - Проверка подписи
   - Извлечение данных пользователя

4. **Уведомления**
   - Уведомления о новых заказах
   - Уведомления об изменении статуса заказа

### YooKassa (Платежи)

**Конфигурация:** Через админ-панель (`/admin/payment-settings/yookassa`)

**Функциональность:**

1. **Создание платежа**
   - Генерация ссылки на оплату
   - Сохранение платежа в БД

2. **Webhook обработка**
   - Получение уведомлений о статусе платежа
   - Автоматическое обновление статуса заказа

3. **Управление настройками**
   - Shop ID
   - Secret Key
   - Тестовый/боевой режим

---

## 📝 Правила работы с Cursor

### Обязательные правила

1. **Всегда обновляй документацию**
   - При изменении структуры файлов
   - При добавлении новых функций
   - При изменении API
   - При изменении логики работы
   - При изменении визуальной части

2. **Используй существующие паттерны**
   - Следуй структуре проекта
   - Используй существующие компоненты и утилиты
   - Придерживайся стиля кода

3. **Проверяй зависимости**
   - Перед добавлением новых зависимостей проверь, не существует ли аналог
   - Используй существующие библиотеки

4. **Тестируй изменения**
   - Проверяй работу на всех страницах
   - Проверяй мобильную версию
   - Проверяй интеграции

### Структура промптов

При создании промптов для изменений используй следующий формат:

1. **Описание задачи** - Что нужно сделать
2. **Контекст** - Где это находится (файл, компонент, страница)
3. **Требования** - Какие требования нужно выполнить
4. **Примеры** - Примеры из существующего кода (если есть)

### Правила именования

**Backend:**
- Контроллеры: `PascalCase` (например, `CategoryController`)
- Модели: `PascalCase` (например, `Product`)
- Сервисы: `PascalCase` (например, `TelegramService`)
- Middleware: `PascalCase` (например, `ForceHttps`)

**Frontend React:**
- Компоненты: `PascalCase` (например, `ProductCard`)
- Файлы компонентов: `PascalCase.tsx` (например, `ProductCard.tsx`)
- Hooks: `camelCase` с префиксом `use` (например, `useProducts`)
- Stores: `camelCase` с суффиксом `Store` (например, `cartStore`)

**Frontend Vue:**
- Компоненты: `PascalCase.vue` (например, `Products.vue`)
- Страницы: `PascalCase.vue` (например, `CategoryEdit.vue`)

### Работа с API

1. **Публичные endpoints** - GET запросы к категориям и продуктам
2. **Защищенные endpoints** - Все POST/PUT/DELETE запросы требуют токен
3. **Формат ответа** - Всегда `{ data: ... }`
4. **Обработка ошибок** - Используй существующие паттерны

### Работа с состояниями

**React (Zustand):**
- Используй существующие stores
- Если нужен новый store - создай по аналогии с существующими
- Сохраняй критичные данные в localStorage

**Vue:**
- Используй существующие паттерны работы с данными
- API клиент через `utils/api.js`

---

## 🎯 Шаблоны промптов

### Шаблон 1: Добавление новой функции

```
Нужно добавить [описание функции] в [где находится].

Контекст:
- Файл: [путь к файлу]
- Компонент/Контроллер: [название]
- Связанные файлы: [список файлов]

Требования:
1. [требование 1]
2. [требование 2]
3. [требование 3]

Примеры из существующего кода:
- [ссылка на похожий код]

После реализации обнови APPLICATION_DOCUMENTATION.md
```

### Шаблон 2: Изменение существующей функции

```
Нужно изменить [описание функции] в [где находится].

Текущее поведение:
- [описание текущего поведения]

Новое поведение:
- [описание нового поведения]

Контекст:
- Файл: [путь к файлу]
- Метод/Функция: [название]

Изменения:
1. [изменение 1]
2. [изменение 2]

После реализации обнови APPLICATION_DOCUMENTATION.md
```

### Шаблон 3: Исправление бага

```
Найден баг в [где находится].

Описание проблемы:
- [описание проблемы]
- Шаги для воспроизведения: [шаги]

Ожидаемое поведение:
- [что должно происходить]

Фактическое поведение:
- [что происходит сейчас]

Контекст:
- Файл: [путь к файлу]
- Компонент/Контроллер: [название]

После исправления обнови APPLICATION_DOCUMENTATION.md, если это влияет на логику
```

### Шаблон 4: Добавление новой страницы/компонента

```
Нужно добавить новую страницу/компонент [название].

Контекст:
- Тип: [страница/компонент]
- Расположение: [директория]
- Маршрут: [маршрут, если страница]

Функциональность:
1. [функция 1]
2. [функция 2]

Дизайн:
- [описание дизайна или ссылка на макет]

Примеры для вдохновения:
- [существующие похожие компоненты/страницы]

После реализации обнови APPLICATION_DOCUMENTATION.md в соответствующих разделах
```

### Шаблон 5: Изменение API

```
Нужно изменить/добавить API endpoint [название].

Текущий endpoint (если изменение):
- Метод: [GET/POST/PUT/DELETE]
- Путь: [путь]
- Текущее поведение: [описание]

Новый/Измененный endpoint:
- Метод: [GET/POST/PUT/DELETE]
- Путь: [путь]
- Параметры: [параметры]
- Ответ: [формат ответа]

Контекст:
- Контроллер: [название]
- Метод: [название метода]
- Маршрут: [файл routes/api.php]

После реализации обнови APPLICATION_DOCUMENTATION.md в разделе API
```

---

## 🔄 Процесс обновления документации

При внесении изменений в проект:

1. **Сделай изменения в коде**
2. **Обнови соответствующий раздел в APPLICATION_DOCUMENTATION.md**
3. **Проверь, что документация актуальна**
4. **Убедись, что примеры кода в документации работают**

**Разделы для обновления:**

- **Изменения в Backend:**
  - Раздел "Backend (Laravel)"
  - Модели, контроллеры, сервисы, маршруты

- **Изменения в React MiniApp:**
  - Раздел "Frontend - React MiniApp"
  - Компоненты, страницы, состояние, API

- **Изменения в Vue Admin:**
  - Раздел "Frontend - Vue Admin Panel"
  - Страницы, компоненты

- **Изменения в интеграциях:**
  - Раздел "Интеграции"
  - Telegram, платежи

- **Новые правила:**
  - Раздел "Правила работы с Cursor"
  - Шаблоны промптов

---

## 📌 Важные заметки

1. **Два фронтенда:**
   - React MiniApp (`frontend/`) - для пользователей Telegram
   - Vue Admin Panel (`resources/js/`) - для администраторов

2. **Публичные API:**
   - GET запросы к категориям и продуктам - публичные (для miniApp)
   - Остальные запросы требуют аутентификацию

3. **Оптимизация изображений:**
   - Автоматическая конвертация в WebP
   - Генерация вариантов размеров
   - Использование компонента OptimizedImage

4. **Telegram интеграция:**
   - MiniApp открывается через кнопку в боте
   - Использует Telegram WebApp API
   - Валидация initData на бэкенде

5. **Состояние:**
   - React: Zustand stores
   - Vue: API клиент + локальное состояние

---

**Последнее обновление:** 2026-01-04  
**Версия документации:** 1.0

