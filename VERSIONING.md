# Версионирование проекта

## Текущая версия: v1.0.0

## 📦 Установка определенной версии

Проект использует семантическое версионирование (Semantic Versioning). Для установки конкретной версии используйте git теги.

### Как установить определенную версию

#### Вариант 1: Клонировать сразу нужную версию

Этот вариант наиболее быстрый и рекомендуется для новых установок. Клонируется только указанная версия без всей истории репозитория.

```bash
# Клонировать сразу версию 1.0.0
git clone --branch v1.0.0 --depth 1 https://github.com/letoceiling-coder/express.git

# Для другой версии просто замените номер:
git clone --branch v1.1.0 --depth 1 https://github.com/letoceiling-coder/express.git
git clone --branch v2.0.0 --depth 1 https://github.com/letoceiling-coder/express.git
```

**Преимущества:**
- Быстрое клонирование (только нужная версия)
- Меньше места на диске
- Готово к использованию сразу после клонирования

#### Вариант 2: В существующем репозитории

Если у вас уже есть клон репозитория, переключитесь на нужную версию:

```bash
# Сначала получить все теги из удаленного репозитория
git fetch --tags

# Переключиться на нужную версию
git checkout v1.0.0

# Для другой версии:
git checkout v1.1.0
git checkout v2.0.0
```

**Если нужно вернуться на последнюю версию (main):**
```bash
git checkout main
git pull origin main
```

**Важно:** После переключения на конкретную версию, вы находитесь в состоянии "detached HEAD". Для работы рекомендуется создать новую ветку:
```bash
git checkout -b v1.0.0-work v1.0.0
```

### После установки версии

После установки конкретной версии выполните стандартную установку:

```bash
# Установить зависимости PHP
composer install

# Установить зависимости Node.js
npm install

# Скопировать файл окружения
cp .env.example .env

# Сгенерировать ключ приложения
php artisan key:generate

# Выполнить миграции
php artisan migrate

# Собрать фронтенд (если нужно)
npm run build:admin
```

## 🔍 Просмотр доступных версий

Перед установкой конкретной версии полезно посмотреть список всех доступных версий:

```bash
# Список всех тегов (версий)
git tag -l

# Список тегов с сортировкой (от новых к старым)
git tag -l --sort=-v:refname

# Показать последние 10 версий
git tag -l --sort=-v:refname | head -10

# Информация о конкретной версии (коммит, автор, дата)
git show v1.0.0

# История изменений между версиями
git log v1.0.0..v1.1.0 --oneline
```

## 🏷️ Создание новых версий

Проект использует семантическое версионирование (Semantic Versioning): `MAJOR.MINOR.PATCH`

- **MAJOR** (X.0.0) - несовместимые изменения API, breaking changes
- **MINOR** (0.X.0) - новая функциональность, обратно совместимая
- **PATCH** (0.0.X) - исправления ошибок, обратно совместимые

### Процесс создания новой версии

**ВАЖНО:** Перед созданием тега обязательно обновите документацию `VERSIONING.md`!

1. **Обновите документацию:**
   - Добавьте новую версию в раздел "История версий"
   - Обновите "Текущая версия" в начале файла
   - Закоммитьте изменения

2. **Создайте git tag:**
   ```bash
   # Сначала убедитесь, что все изменения закоммичены
   git status
   
   # Закоммитьте изменения документации (если есть)
   git add VERSIONING.md
   git commit -m "docs: обновлена документация для версии X.Y.Z"
   ```

3. **Создайте тег:**
   ```bash
   # Патч-обновление (v1.0.0 → v1.0.1)
   git tag -a v1.0.1 -m "Version 1.0.1 - описание исправлений"
   
   # Минорное обновление (v1.0.0 → v1.1.0)
   git tag -a v1.1.0 -m "Version 1.1.0 - описание новых возможностей"
   
   # Мажорное обновление (v1.0.0 → v2.0.0)
   git tag -a v2.0.0 -m "Version 2.0.0 - описание критических изменений"
   ```

4. **Отправьте тег в удаленный репозиторий:**
   ```bash
   # Отправить один тег
   git push origin v1.1.0
   
   # Или отправить все теги сразу
   git push origin --tags
   ```

### Пример создания версии v1.1.0

```bash
# 1. Убедитесь, что все изменения закоммичены
git status

# 2. Обновите VERSIONING.md (добавьте новую версию в историю)
# ... редактирование файла ...

# 3. Закоммитьте изменения документации
git add VERSIONING.md
git commit -m "docs: добавлена версия v1.1.0 в документацию"

# 4. Создайте тег с аннотацией
git tag -a v1.1.0 -m "Version 1.1.0 - Новые возможности медиа-библиотеки и улучшения производительности"

# 5. Отправьте тег
git push origin v1.1.0

# 6. Отправьте коммиты (если еще не отправлены)
git push origin main
```


## Удаление версий

```bash
# Удалить локальный тег
git tag -d v1.0.0

# Удалить тег из GitHub
git push origin --delete v1.0.0
```

## ⚠️ Важные моменты

1. **Теги неизменяемы** - тег v1.0.0 всегда будет указывать на один и тот же коммит. Нельзя изменить существующий тег, можно только создать новый.

2. **Ветка main продолжает развиваться** - новые изменения не влияют на зафиксированные версии. Пользователи, использующие конкретную версию, получают стабильный код.

3. **Семантическое версионирование**: MAJOR.MINOR.PATCH
   - **MAJOR (X.0.0)**: несовместимые изменения API, breaking changes, требуется обновление кода при миграции
   - **MINOR (0.X.0)**: новая функциональность, обратно совместимая, можно безопасно обновляться
   - **PATCH (0.0.X)**: исправления ошибок, обратно совместимые, рекомендуется обновляться

4. **Всегда обновляйте документацию** перед созданием нового тега версии.

5. **Тестируйте версию** перед созданием тега - убедитесь, что все работает корректно.

6. **Используйте осмысленные сообщения** при создании тегов - они помогают понять изменения в версии.

## 📝 История версий

Все версии проекта с описанием изменений. Версии отсортированы от новых к старым.

## Будущие версии

### v1.1.0 (планируется) - Управление каталогом товаров

**Статус:** Стабильная версия

**Основные изменения:**
- Добавлено: система управления Telegram ботами в админ-панели
- Добавлено: автоматическая регистрация webhook при создании бота
- Добавлено: проверка установки webhook через Telegram API
- Добавлено: настройка приветственного сообщения для ботов
- Добавлено: настройка параметров webhook (allowed_updates, max_connections, secret_token)
- Добавлено: обработка команды /start с автоматической отправкой приветственного сообщения
- Добавлено: подробное логирование всех операций с ботами и webhook
- Добавлено: публичный роут для приема webhook от Telegram (`/api/telegram/webhook/{id}`)
- Добавлено: команда `php artisan bots:fix-webhook-urls` для исправления URL webhook

**База данных:**
- Добавлена таблица `bots` с полями: name, token, username, webhook_url, webhook_registered, welcome_message, settings (JSON), is_active

**API Endpoints:**
- `GET /api/v1/bots` - список ботов
- `POST /api/v1/bots` - создание бота
- `GET /api/v1/bots/{id}` - информация о боте
- `PUT /api/v1/bots/{id}` - обновление бота
- `DELETE /api/v1/bots/{id}` - удаление бота
- `GET /api/v1/bots/{id}/check-webhook` - проверка webhook
- `POST /api/v1/bots/{id}/register-webhook` - регистрация webhook
- `POST /api/telegram/webhook/{id}` - публичный endpoint для приема обновлений от Telegram

**Компоненты:**
- `app/Models/Bot.php` - модель бота
- `app/Http/Controllers/Api/BotController.php` - контроллер управления ботами
- `app/Services/TelegramService.php` - сервис для работы с Telegram Bot API
- `resources/js/pages/admin/Bots.vue` - компонент админ-панели для управления ботами
- `app/Console/Commands/FixBotWebhookUrls.php` - команда для исправления URL webhook

**Миграции:**
- `2025_12_26_223150_create_bots_table.php` - создание таблицы bots

**Установка:**
```bash
git clone --branch v1.1.0 --depth 1 https://github.com/letoceiling-coder/express.git
```

---

### v1.0.0 (2025-01-20) - Initial release

**Статус:** Стабильная версия

**Основные возможности:**
- Telegram Mini App с React frontend
- Laravel 11 backend API
- Vue 3 админ-панель для управления
- Система медиа-библиотеки с поддержкой папок
- Система поддержки с интеграцией CRM
- Управление пользователями и ролями
- Проверка подписки
- Система уведомлений
- Управление Telegram ботами
- Автоматический деплой через GitHub Actions

**Технологический стек:**
- Backend: Laravel 11, PHP 8.2+
- Frontend (Admin): Vue 3 + TypeScript + Vite
- Frontend (MiniApp): React 18 + TypeScript
- База данных: MySQL/PostgreSQL
- Авторизация: Laravel Sanctum

**Установка:**
```bash
git clone --branch v1.0.0 --depth 1 https://github.com/letoceiling-coder/express.git
```

---

## 🔄 Обновление документации при изменении версий

**ВАЖНО:** При каждом создании новой версии обязательно обновляйте этот файл `VERSIONING.md`.

### Правила обновления документации

1. **При создании новой версии:**
   - Обновите раздел "Текущая версия" в начале файла
   - Добавьте новую запись в раздел "История версий" с полным описанием изменений
   - Укажите дату релиза
   - Перечислите все новые возможности, изменения и исправления

2. **Формат записи версии:**
   ```markdown
   ### vX.Y.Z (ГГГГ-ММ-ДД) - Краткое описание
   
   **Статус:** Стабильная/Beta/Alpha
   
   **Основные изменения:**
   - Добавлено: описание новых функций
   - Изменено: описание изменений
   - Исправлено: описание исправлений
   - Удалено: описание удаленного функционала (если есть)
   
   **Критические изменения (если есть):**
   - Несовместимые изменения API
   - Изменения в структуре БД
   - Требования к обновлению
   
   **Установка:**
   ```bash
   git clone --branch vX.Y.Z --depth 1 https://github.com/letoceiling-coder/express.git
   ```
   ```

3. **Что включать в описание версии:**
   - Все новые функции и возможности
   - Значительные изменения в существующей функциональности
   - Критические исправления ошибок
   - Изменения в зависимостях (PHP, Laravel, npm пакеты)
   - Миграции базы данных
   - Изменения в конфигурации
   - Совместимость (breaking changes)

4. **Чеклист перед релизом новой версии:**
   - [ ] Все изменения зафиксированы и запушены
   - [ ] Тесты проходят успешно
   - [ ] Документация обновлена
   - [ ] Раздел "История версий" дополнен новой версией
   - [ ] Раздел "Текущая версия" обновлен
   - [ ] Создан git tag с правильной версией
   - [ ] Tag запушен в удаленный репозиторий

5. **Пример правильного обновления:**
   
   Перед созданием новой версии v1.1.0:
   
   ```markdown
   ## Текущая версия: v1.1.0
   ```
   
   И добавление в историю:
   
   ```markdown
   ### v1.1.0 (2025-12-28) - Новые возможности медиа-библиотеки
   
   **Статус:** Стабильная версия
   
   **Основные изменения:**
   - Добавлено: пакетная загрузка файлов
   - Добавлено: поиск по медиа-библиотеке
   - Изменено: улучшена производительность загрузки больших файлов
   - Исправлено: ошибка при удалении папок с файлами
   
   **Миграции:**
   - Добавлена таблица media_tags для улучшения поиска
   
   **Установка:**
   ```bash
   git clone --branch v1.1.0 --depth 1 https://github.com/letoceiling-coder/express.git
   ```
   ```

### Автоматическое отслеживание изменений

Для отслеживания изменений между версиями можно использовать:

```bash
# Посмотреть коммиты между версиями
git log v1.0.0..v1.1.0 --oneline

# Посмотреть изменения в файлах
git diff v1.0.0..v1.1.0 --stat

# Посмотреть список измененных файлов
git diff --name-only v1.0.0..v1.1.0
```

Эти команды помогут при составлении описания новой версии.

